Name = {{ host }}
AddressFamily = any
Mode = switch

# Nodes
{% if tinc['network'][network]['master'][host] is defined %}
{% for node,node_setting in tinc['network'][network]['node'] %}
ConnectTo = {{ node }}
{% endfor %}
{% elif tinc['network'][network]['node'][host] is defined %}
{% for master,master_setting in tinc['network'][network]['master'] %}
ConnectTo = {{ master }}
{% endfor %}
{% endif %}
