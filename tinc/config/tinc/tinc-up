#!/bin/sh
ifconfig $INTERFACE {%- if pillar['tinc'][network]['master'][node] is defined -%}
{{ pillar['tinc'][network]['master'][node]['local-ip'] }}
{%- else -%}
{{ pillar['tinc'][network]['node'][node]['local-ip'] }}
{%- endif -%} netmask 255.255.0.0
