#!/bin/sh
ip addr add {{ tinc['network'][network][nodetype][grains['id']]['local-ip'] + "/" }}
{%- if tinc['network'][network][nodetype][grains['id']]['conf'] is defined and tinc['network'][network][nodetype][grains['id']]['conf']['host'] is defined and tinc['network'][network][nodetype][grains['id']]['conf']['host']['Subnet'] is defined -%}
{{ tinc['network'][network][nodetype][grains['id']]['conf']['host']['Subnet'].split('/')[1] + " " }}
{%- else -%}
{{ tinc['network'][network]['conf']['host']['Subnet'].split('/')[1] + " " }}
{%- endif -%}
dev $INTERFACE

ip link set $INTERFACE up
